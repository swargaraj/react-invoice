# server-pdf

Example app demonstrating server-side PDF generation with `react-invoice` using Bun.

## Setup

```bash
bun install
```

## Run

```bash
bun run index.tsx
```

Generates `./out/invoice.pdf`.
